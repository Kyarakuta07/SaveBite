import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App bootstrap test', (WidgetTester tester) async {
    // Placeholder — per skill: write widget tests after each feature.
    expect(1 + 1, 2);
  });
}
