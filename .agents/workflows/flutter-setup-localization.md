---
name: flutter-setup-localization
description: "Add `flutter_localizations` and `intl` dependencies, enable \"generate true\" in `pubspec.yaml`, and create an `l10n.yaml` configuration file. Use when initializing localization support for a new Flutter project."
version: 1.0.0
---

# Activate Skill: flutter-setup-localization

Please read and internalize the skill documentation located at:
**`c:/xampp/htdocs/Savebite/.agents/skills/flutter-setup-localization/SKILL.md`**

(And any other relevant files in that directory)

## Mission
Add `flutter_localizations` and `intl` dependencies, enable "generate true" in `pubspec.yaml`, and create an `l10n.yaml` configuration file. Use when initializing localization support for a new Flutter project.

## Instructions
1.  **Load Context**: Read the file path provided above.
2.  **Activate Persona**: Adopt the role and expertise defined in that document.
3.  **Execute**: Await further user instructions acting as this expert.
